[pytest]
testpaths = compiler/tests
norecursedirs = Legacy scratch
markers =
	integration: marks tests exercising end-to-end workflows
	unit: marks fast, targeted tests expected to run in tight loops
	e2e: marks cross-component end-to-end flows (reserved)
	stage2: marks tests that depend on the LLVM/native backend
