[capsule]
name = "sfn/time"
version = "0.1.1"
description = "Time and scheduling primitives for Sailfin"

[dependencies]

[capabilities]
required = ["clock"]

[build]
entry = "src/mod.sfn"
kind = "library"
