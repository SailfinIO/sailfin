[capsule]
name = "sfn/time"
version = "0.2.0"
description = "Time and scheduling primitives for Sailfin"

[dependencies]

[capabilities]
required = ["clock"]

[build]
entry = "src/mod.sfn"
kind = "library"
