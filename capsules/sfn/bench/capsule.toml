[capsule]
name = "sfn/bench"
version = "0.2.0"
description = "In-language microbenchmark harness for Sailfin (the parallel to sfn/test)"

[dependencies]

[capabilities]
required = ["clock", "io"]

[build]
entry = "src/mod.sfn"
kind = "library"
