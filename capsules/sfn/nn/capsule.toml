[capsule]
name = "sfn/nn"
version = "0.1.1"
description = "Neural network training and inference utilities for Sailfin"

[dependencies]

[capabilities]
required = ["gpu"]

[build]
entry = "src/mod.sfn"
