[capsule]
name = "sfn/tensor"
version = "0.1.1"
description = "Tensor primitives and operations for Sailfin"

[dependencies]

[capabilities]
required = ["gpu"]

[build]
entry = "src/mod.sfn"
kind = "library"
