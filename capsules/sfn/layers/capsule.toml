[capsule]
name = "sfn/layers"
version = "0.1.2"
description = "Neural network layer building blocks for Sailfin"

[dependencies]

[capabilities]
required = ["gpu"]

[build]
entry = "src/mod.sfn"
kind = "library"
