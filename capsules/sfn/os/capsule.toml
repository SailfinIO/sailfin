[capsule]
name = "sfn/os"
version = "0.4.0"
description = "Environment, process, and OS interaction for Sailfin"

[dependencies]

[capabilities]
required = ["io"]

[build]
entry = "src/mod.sfn"
kind = "library"
