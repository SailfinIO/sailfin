[capsule]
name = "sfn/os"
version = "0.2.0"
description = "Environment, process, and OS interaction for Sailfin"

[dependencies]

[capabilities]
required = ["io"]

[build]
entry = "src/mod.sfn"
kind = "library"
