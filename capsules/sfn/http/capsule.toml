[capsule]
name = "sfn/http"
version = "0.10.0"
description = "HTTP client and server for Sailfin"

[dependencies]

[capabilities]
required = ["io", "net"]

[build]
entry = "src/mod.sfn"
kind = "library"
