[capsule]
name = "sfn/cli"
version = "0.2.0"
description = "CLI argument parsing and terminal output for Sailfin"

[dependencies]

[capabilities]
required = ["io"]

[build]
entry = "src/mod.sfn"
kind = "library"
