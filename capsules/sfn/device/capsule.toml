[capsule]
name = "sfn/device"
version = "0.1.0"
description = "Capability-gated accelerator device dispatch for Sailfin"

[dependencies]

[capabilities]
required = ["gpu"]

[build]
entry = "src/mod.sfn"
kind = "library"
