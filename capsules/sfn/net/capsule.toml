[capsule]
name = "sfn/net"
version = "0.1.2"
description = "Low-level networking primitives for Sailfin"

[dependencies]

[capabilities]
required = ["net", "io"]

[build]
entry = "src/mod.sfn"
kind = "library"
