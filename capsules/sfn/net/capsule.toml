[capsule]
name = "sfn/net"
version = "0.2.0"
description = "Low-level networking primitives for Sailfin"

[dependencies]

[capabilities]
required = ["net", "io"]

[build]
entry = "src/mod.sfn"
kind = "library"
