[capsule]
name = "sfn/sync"
version = "0.1.1"
description = "Concurrency and synchronization primitives for Sailfin"

[dependencies]

[capabilities]
required = ["io"]

[build]
entry = "src/mod.sfn"
kind = "library"
