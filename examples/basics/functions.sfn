// examples/basics/functions.sfn

fn add(x: number, y: number) -> number {
  return x + y;
}

fn greet(name: string = "Sailfin") -> string {
  return "Hello, {{name}}!";
}

fn main() ![io] {
  print(add(3, 5)); // Outputs: 8
  print(greet());   // Outputs: Hello, Sail!
  print(greet("Alice")); // Outputs: Hello, Alice!
}
