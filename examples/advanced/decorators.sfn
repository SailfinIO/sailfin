// examples/advanced/decorators.sfn
import { logExecution } from "runtime/prelude";

@logExecution fn compute(x: int) -> int ![io] { return x * x; }

fn main() ![io] {
    let value = compute(5);
    print("compute(5) = {{value}}");
}
