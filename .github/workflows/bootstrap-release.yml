name: "[DEPRECATED] Bootstrap Compiler Build and Publish"

# This workflow is deprecated in favor of sailfin-release.yml
# TODO: Remove this file after self-hosting transition is complete

on:
  workflow_dispatch:
    inputs:
      force_run:
        description: "Force run (deprecated workflow)"
        required: false
        type: boolean
        default: false

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    if: github.event.inputs.force_run == 'true'

    steps:
      - name: Deprecated Workflow Notice
        run: |
          echo "❌ This workflow is deprecated!"
          echo "🚢 Use the new self-hosting pipeline: sailfin-release.yml"
          echo "📄 See: .github/workflows/sailfin-release.yml"
          exit 1
