name: "[DEPRECATED] Release (Bootstrap-based)"

# This workflow is deprecated in favor of sailfin-release.yml
# TODO: Remove this file after self-hosting transition is complete

on:
  workflow_dispatch:
    inputs:
      force_run:
        description: "Force run deprecated workflow"
        required: false
        type: boolean
        default: false

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    if: github.event.inputs.force_run == 'true'

    steps:
      - name: Deprecated Workflow Notice
        run: |
          echo "❌ This workflow is deprecated!"

          echo "🚢 Use the new self-hosting release pipeline:"
          echo "📄 .github/workflows/sailfin-release.yml"
          echo ""
          echo "To create a new self-hosting release:"
          echo "1. Create a git tag: git tag v1.0.0"
          echo "2. Push the tag: git push origin v1.0.0"
          echo "3. Or use workflow_dispatch on sailfin-release.yml"
          exit 1
