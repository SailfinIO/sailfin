[capsule]
name = "sfn/log"
version = "0.3.0"
description = "Structured, leveled logging for Sailfin"

[dependencies]

[capabilities]
required = ["io", "clock"]

[build]
entry = "src/mod.sfn"
kind = "library"
