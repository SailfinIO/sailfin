struct DivisionError {
    message: string;
}

fn main() -> void {
    let x = new DivisionError { message: "test" };
    
    match x {
        DivisionError { message } => print.info("Error"),
    }
}
