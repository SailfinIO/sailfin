fn shared_add(a: number, b: number) -> number {  // alias-coverage: cross-module fixture preserves number-aliased API
    return a + b;
}

fn shared_greet(name: string) -> string { return "hello, " + name; }

export { shared_add, shared_greet };
