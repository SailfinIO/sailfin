[capsule]
name = "fixture/stateful-http-users-server"
version = "0.0.0"
description = "stateful sfn/http users server e2e fixture"

[dependencies]
"sfn/http" = "*"

[capabilities]
required = ["io", "net"]

[build]
entry = "src/main.sfn"
kind = "binary"
