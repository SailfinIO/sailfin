[capsule]
name = "sfn/analyzer"
version = "0.10.2"
description = "Sailfin semantic analysis pipeline (toolchain-internal)"
publish = false

[dependencies]
"sfn/syntax" = "*"
"sfn/ir" = "*"
"sfn/strings" = "*"

[capabilities]
required = []

[build]
kind = "library"
entry = "src/mod.sfn"
