[capsule]
name = "sfn/syntax"
version = "0.10.1"
description = "Sailfin syntax tree, lexer, and parser (toolchain-internal)"
publish = false

[dependencies]
"sfn/strings" = "*"

[capabilities]
required = []

[build]
kind = "library"
entry = "src/mod.sfn"
