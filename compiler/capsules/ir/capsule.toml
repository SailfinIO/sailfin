[capsule]
name = "sfn/ir"
version = "0.10.5"
description = "Sailfin native, typed SSA, and tensor IR contracts (toolchain-internal)"
publish = false

[dependencies]
"sfn/strings" = "*"

[capabilities]
required = []

[build]
kind = "library"
entry = "src/mod.sfn"
